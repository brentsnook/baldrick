# don't give this file a .rb extension or cucumber will try to require it resulting in the server starting

require File.dirname(__FILE__) + '/../../lib/baldrick_serve'
require File.dirname(__FILE__) + '/../steps/helpers/command_output'

serve_every 0.5
listen_to :injour

to /follow an order/ do
  CommandOutput.add 'Order followed'
end